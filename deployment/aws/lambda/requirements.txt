# Minimal dependencies for Lambda (use Lambda layers for heavy packages)
boto3>=1.28.0
pandas>=2.0.0
pyarrow>=12.0.0
numpy>=1.24.0

# scPerturb-CMap will be packaged separately or as a layer
# Uncomment if bundling everything:
# scperturb-cmap==0.2.0
