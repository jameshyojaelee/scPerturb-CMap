# Repository Structure

For a directory-by-directory breakdown see the live
[`STRUCTURE.md`](https://github.com/jameslee/scPerturb-CMap/blob/main/STRUCTURE.md)
file in the root of the repository. Highlights:

- `src/scperturb_cmap/` – Python package code (API, CLI, models, data loaders)
- `docs/` – MkDocs site content
- `examples/` – Demo assets and sample scripts
- `case_studies/` – Reproducible workflows for published analyses
- `deployment/` – Docker, Helm, and cloud automation assets
- `tests/` – Unit and integration test suite

Use this page as a quick reminder while browsing the documentation; follow the
link above for the full, auto-generated tree.
